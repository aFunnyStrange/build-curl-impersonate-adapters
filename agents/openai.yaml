interface:
  display_name: "[Draft] Build curl-impersonate Adapters"
  short_description: "Build scenario-complete browser backends"
  default_prompt: "Use $build-curl-impersonate-adapters to compile authorized request-context profiles into one native bundle per target and integrate profile-safe connection pools with curl_cffi and scrapy_cffi."
